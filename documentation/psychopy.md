# Installing psychopy without the gui

Since we will be using psychopy exclusively through sublime, 
```
brew install hdf5
brew install c-blosc
export HDF5_DIR=/opt/homebrew/opt/hdf5 
export BLOSC_DIR=/opt/homebrew/opt/c-blosc
pip install tables
```